---
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "About Me"

const identity = {
	firstName: "Erika",
	country: "USA",
	occupation: "student",
	hobbies: ["anime", "Genshin Impact", "tech", "walking"]
};
const skills = ["HTML", "CSS", "JavaScript", "SQL", "Astro", "Python", "Writing Docs"];

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "navy";
const fontWeight = "bold";
const textCase = "uppercase";
---

<BaseLayout pageTitle={pageTitle}>
	<style is:global define:vars={{skillColor, fontWeight, textCase}}>
			h1 {
				color: purple;
				font-size: 4rem;
			}
			.skill {
				color: var(--skillColor);
				font-weight: var(--fontWeight);
				text-transform: var(--textCase);
			}
		</style>
	<h2>My awesome blog subtitle</h2>
	<p>I'm working through Astro's intro tutorial. This is the 2nd page of the site, and the 1st one I made.</p>
        <p>I'll be updating this site as I progress through the tutorial!</p>

		<p>Here are a few facts about me:</p>
		<ul>
			<li>My name is {identity.firstName}.</li>
			<li>I live in the {identity.country} and I am a {identity.occupation}.</li>
			{identity.hobbies.length >= 2 && 
			<li>Two of my hobbies are: {identity.hobbies[0]} and {identity.hobbies[1]}.</li>}
		</ul>

		<p>My skills are:</p>
		<ul>
			{skills.map((skill) => <li class="skill">{skill}</li>)}
		</ul>

		{happy && <p>I am happy to be learning Astro!</p>}
		{finished && <p>I finished this tutorial!</p>}
		{goal === 3 ? <p>My goal is finish in 3 days.</p> : <p>My goal is not 3 days.</p>}
</BaseLayout>